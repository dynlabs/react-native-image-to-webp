appId: dynlabs.reactnativeimagetowebp.example
---
# Test Image Conversion - 1080p Image
# Tests: Convert 1080p test image, verify size difference

- assertVisible: "Image to WebP Converter"

# Scroll to manual path input section
- scroll
- assertVisible:
    text: "Manual Path"
    optional: true

# Enter the path to 1080p test image
- tapOn:
    text: ".*"
    optional: true
- scroll
- inputText: "/sdcard/Download/test-images/test-1080p.jpg"
- hideKeyboard

# Wait for image to load
- waitForAnimationToEnd:
    timeout: 3000
- assertVisible:
    text: "Original Size:"
    optional: true

# Scroll to convert section
- scroll
- assertVisible: "Convert (balanced)"
- tapOn: "Convert (balanced)"

# Wait for conversion to complete
- waitForAnimationToEnd:
    timeout: 15000

# Verify success
- assertVisible: "Success"
- tapOn: "OK"

# Verify results section shows conversion details
- scroll
- assertVisible: "Results"
- assertVisible: "balanced"

# Verify size information
- assertVisible: "WebP Size:"
- assertVisible:
    text: "Saved:|Increased:"
    optional: true

# Verify dimensions are shown
- assertVisible:
    text: "Dimensions:.*×.*"
