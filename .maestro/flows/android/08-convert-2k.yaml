appId: dynlabs.reactnativeimagetowebp.example
---
# Test Image Conversion - 2K Image
# Tests: Convert 2K test image, verify size difference

- assertVisible: "Image to WebP Converter"

# Scroll to manual path input section
- scroll
- assertVisible:
    text: "Manual Path"
    optional: true

# Enter the path to 2K test image
- tapOn:
    text: ".*"
    optional: true
- scroll
- inputText: "/sdcard/Download/test-images/test-2k.jpg"
- hideKeyboard

# Wait for image to load
- waitForAnimationToEnd:
    timeout: 3000
- assertVisible:
    text: "Original Size:"
    optional: true

# Scroll to convert section
- scroll
- assertVisible: "Convert (balanced)"
- tapOn: "Convert (balanced)"

# Wait for conversion to complete
- waitForAnimationToEnd:
    timeout: 15000

# Verify success
- assertVisible: "Success"
- tapOn: "OK"

# Verify results section
- scroll
- assertVisible: "Results"
- assertVisible: "balanced"

# Verify size difference
- assertVisible: "WebP Size:"
- assertVisible:
    text: "Saved:|Increased:"
    optional: true
